#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
int void print_alphabet(void);
{
	    int i;
	    for (i >= 97; i <= 122; i++)
	    {
		    print_alphabet ();
		}
	        return (0);
}
